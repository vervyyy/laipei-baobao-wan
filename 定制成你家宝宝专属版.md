# 怎么用这份文件

这个项目本来是照着我自己家宝宝（19个月）做的。如果你也想要一份"专属于你家宝宝"的版本，不需要自己从头写代码，跟着下面3步走：

1. 打开GitHub仓库 [laipei-baobao-wan](https://github.com/vervyyy/laipei-baobao-wan)，点绿色 **Code** 按钮 → **Download ZIP**，解压到你电脑上任意位置。
2. 用 **Trae** 或 **CodeBuddy**（或任何能读写本地文件、能执行命令的AI编程工具）打开解压出来的这个文件夹。
3. 在AI聊天框里打一句话就行：

   > 读一下这个文件夹里的《定制成你家宝宝专属版.md》，照着里面说的做。

   这个文件本来就在你刚才解压的文件夹里，AI直接读本地文件就行，不用联网、不用你自己复制粘贴。它会先问你几个关于宝宝的问题，回答完就会开始帮你改。

   （如果AI没反应、或者没主动去读这个文件，把下面「===== 从这里开始复制，粘贴给AI ===== 」到文末的**全部内容**直接复制粘贴到对话框里发出去，效果是一样的，这是备用方案。）

改完之后怎么运行、怎么用手机同步看，AI会告诉你；如果它没说，直接问它"怎么打开这个App"就行。

---

===== 从这里开始复制，粘贴给AI =====

## 你是谁、要做什么

你现在在帮我改造一个已经写好、能正常运行的宝宝早教App，代码就在你能读写的这个文件夹里。这不是一个从零开始的项目——它是我从一个内容博主那里拿到的成品，代码是完整的、测试过的、能跑的。你的任务是**在不破坏现有功能的前提下，把内容和细节改成属于我家宝宝的**，而不是重写或重新设计这个App。

请先完整读一遍这几个文件，搞清楚现在的结构，再开始动手：`README.md`、`server.py`、`index.html`、`app.js`、`style.css`、`content.json`。

## 这个项目的文件结构（你需要知道的）

- `server.py` —— 本地服务器，零依赖，跑起来之后手机和电脑访问同一个地址、共用同一份数据。里面几乎不需要改，除非我明确要求。
- `content.json` —— **这是你主要要改的文件**。里面装着：适用月龄（`ageLabel`）、各能区的发展阶段清单（`grossStages`/`fineStages`/`langStages`/`cogStages`/`engStages`）、活动库（`modules`）、主题游戏（`themes`）、养育原则种子内容（`principles`）、情景话术种子内容（`scripts`）、安全检查清单和急救卡（`safetyChecklist`/`safetyReminders`/`firstAidCards`）、今日小课的开场收尾仪式（`lessonRitual`）。
- `index.html` —— 页面结构和固定文案（标题、各页面副标题）。
- `app.js` —— 所有交互逻辑（今日小课怎么生成、里程碑怎么筛选活动、主题怎么选、数据怎么存）。**除非我明确要求改功能逻辑，否则不要动这个文件**，改内容主要靠 `content.json`。
- `style.css` —— 视觉样式（手绘蜡笔风格的配色、卡片、字体）。
- `双击打开.html` —— 单文件离线预览版，是用 `build_preview.py` 从上面几个文件自动打包出来的，**不要手动改这个文件**，改完 `index.html`/`app.js`/`content.json`/`style.css` 之后运行一次 `python3 build_preview.py` 重新生成它。
- `data.json` —— 我自己用这个App产生的真实数据（宝宝进度、日记、玩具清单等），这个文件不在仓库里，运行时自动生成，跟你现在要做的定制无关，不要碰。

## 很重要：很多定制根本不需要改代码

这个App里，下面这些内容**用户自己在App里点一点、填一填就能加/改/删**，完全不用你写代码：
- 添加/删除养育原则和情景话术（"养育原则"页面）
- 添加/删除自定义活动，带材料和步骤（"活动库"和"灵感墙"页面）
- 添加/删除玩具和绘本记录（"玩具/绘本记录"页面）
- 勾选宝宝已经会的发展阶段（"宝宝进度"页面），系统会自动不再推荐已掌握阶段的活动
- 收起用不上的模块，比如不需要英文启蒙（"灵感墙"里每个模块卡片上）
- 手动写日记、加入本周计划

**所以你的第一件事，是问我：**
"你是想要更深度的定制（比如整体换一个年龄段的内容、大幅改配色风格、把内置的种子内容换一批），还是只是想要一个属于你家宝宝的基础版本（改个名字、加几个真正符合你家宝宝兴趣的主题游戏），剩下的等App跑起来之后我自己在App里加？"

根据我的回答，再决定要改多少。**如果我没有特别说要大改，默认只做下面"基础定制清单"里的内容，不要自作主张大改架构、大改设计。**

## 关于我家宝宝的信息（回答完这些我们再开始）

请先问我这些问题，等我回答完再动手改（如果我已经在这段话后面直接写了，就不用再问一遍）：

1. 宝宝的小名是什么？（可以选着把标题里通用的"宝宝"换成小名，比如"来陪果果玩"，不换也可以，就用通用版）
2. 宝宝现在多大了？（精确到月，比如"14个月"）
3. 宝宝是男孩还是女孩？（个别用词可能要调整，比如"他/她"）
4. 宝宝最近特别喜欢的3-5件事/东西是什么？（比如某种动物、某个绘本角色、某类声音、某种玩具——这些会被专门加成一两个"主题游戏"或活动，让它看起来是真的为这个宝宝做的，而不是模板堆出来的）
5. 需不需要保留"英文启蒙"这个模块？如果宝宝家里不打算做双语启蒙，可以直接建议我用App里"收起"这个功能关掉，不用改代码。
6. App现在内置的月龄段是18-24个月（1.5-2岁）。宝宝的月龄如果跟这个差得不多（比如12-30个月之间），大概调整一下阶段描述和活动难度就够了；如果差得比较多（比如6个月以内，或者3岁以上），需要提醒我：**这部分内容涉及真实的儿童发展依据和安全信息（尤其是急救卡、发展里程碑），你不能凭空编，需要有依据地调整或者明确告诉我"这部分我没有把握，建议你自己核实/保留原样"**，不能为了"看起来完成了"就瞎编内容。

## 基础定制清单（默认只做这些）

1. **改名字**（可选）：如果想把标题里通用的"宝宝"换成宝宝的小名，需要改的地方包括但不限于：`index.html` 里的标题、`manifest.json` 的 `name`/`short_name`、`server.py` 里的启动打印文字、`README.md` 标题。改完记得重新运行 `python3 build_preview.py`。
2. **加1-2个专属主题游戏**：参考 `content.json` 里 `themes` 数组已有的写法（每个主题有 `id`/`name`/`icon`/`color`/`activities`/`materials`），根据我说的宝宝真实兴趣，新加一两个主题进去，风格要跟现有的一致（活动要具体可操作，材料按"家居物品/食物类/进阶可选/DIY"分类）。如果需要新图标，在 `app.js` 的 `ICONS` 里加一个简单的线条SVG图标，风格要跟已有图标一致（简笔画风格，`stroke="currentColor"`）。
3. **月龄微调**（如果宝宝月龄跟18-24个月差别不大）：把 `content.json` 里 `ageLabel` 改成宝宝的实际月龄段，各 `Stages` 数组里的 `age` 字段按需要微调，不需要整体重写。
4. **不要动**：今日小课的生成逻辑、里程碑筛选逻辑、数据存储方式（本地JSON，不联网）、"不是打卡清单"这个核心原则（不要加进度条、不要加"还剩几个没做"这种文案）、安全须知里的急救卡内容（除非我明确要求你连着可靠信息来源一起核实修改）。

## 改完之后

1. 运行 `python3 build_preview.py`，确认没有报错，生成新的 `双击打开.html`。
2. 告诉我怎么启动这个App（应该是双击 `启动.command`，然后浏览器打开 `http://localhost:8877`；手机在同一个WiFi下访问终端里打印出来的IP地址）。
3. 提醒我：这是纯本地的App，不联网、不上传，数据都存在我自己电脑的 `data.json` 里；如果我想在GitHub上保留自己这份定制，可以自己开一个新仓库或者Fork一份，不强制。

如果中途有任何一步你不确定该怎么改、或者拿不准某个内容是否准确（尤其是宝宝发展相关或者安全相关的信息），**直接停下来问我，不要自己猜着编**。

===== 复制到这里为止 =====
